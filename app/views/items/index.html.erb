<h1>List of items</h1>

<ul>
  <% @items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %> - <%= link_to "❌",
    item_path(item),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
  <% end %>
</ul>

<%= link_to "Add an item", new_item_path %>
